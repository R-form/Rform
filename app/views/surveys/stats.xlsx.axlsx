wb = xlsx_package.workbook
wb.add_worksheet(name: "answers") do |sheet|
  @questionAnswerDatas.each do |questionAnswerData|
    sheet.add_row [questionAnswerData]
  end
  @responseAnswerDatas.each do |responseAnswerData|
    sheet.add_row [responseAnswerData]
  end
end